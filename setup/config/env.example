# Database Configuration
# Copy this file to .env and update with your actual database settings

# PostgreSQL Connection Settings
PGUSER=nndr
PGPASSWORD=nndrpass
PGHOST=localhost
PGPORT=5432
PGDATABASE=nndr_db

# Optional: Different database for testing
# PGDATABASE=nndr_test

# Optional: Connection pool settings
# PGPOOL_SIZE=10
# PGMAX_OVERFLOW=20 