-- Drop OS Open UPRN Staging Table
-- This script drops the existing table to allow recreation with correct schema

-- Drop existing table if it exists
DROP TABLE IF EXISTS public.os_open_uprn_staging; 